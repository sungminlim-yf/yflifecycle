---
name: yf-design-progress
description: "Young Foods B2B 시스템 설계 phase 포인터 — 현재 v0.4, 다음 작업 후보 위치"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1d30d126-8bcb-491b-b40d-e1e58d82e7aa
---

설계 단계 v0.4+ (2026-05-28 갱신). 도메인별 폴더 + 각 폴더 CLAUDE.md 구조 (`onboarding/`, `order-site/`, `airtable/`, `xero/`, `n8n/`). 루트 `CLAUDE.md`가 라우터·대시보드 — 잠긴 결정·미결은 거기에 정리되어 있어 그쪽을 single source로 참조.

**2026-05-31 (회사) — Kumda Cafe 1→3단계 E2E 완주 + 모델 v3 라이브 검증 + 버그 2건**: ① 매직링크 주문 → 오더 #10(`recV89PmcWyVebFoL`) + 라인 ORD-10-KAT + pre-onboarding hold(오더 Hold=true·출하=Hold) ✅. ② Tally 폼 제출 → #7a staging `recSG3xYTfjyatDs5`(submission e5BYPWx, `form status`=**form submitted** 새옵션 typecast 정상) → form status=approved 플립 → **#7b 수동 트리거**(Airtable Automation 미설정 상태라 curl로 webhook 직접 호출) → propagated: Xero Contact `dbddc1ac-8cb7-4e65-b8f9-fdca784f138b` 생성 + 고객행 `recE6GawkFvfwREK3` 완전 채움(Payment=Credit-7days·credit_limit=10000·선호언어 ko) + **오더 #10 hold release(출하=접수)** + HubSpot onboarding=approved + 환영메일(en+ko). ③ **모델 v3 핵심 검증**: 고객 `Onboarding Status` **비어있음**(=#7b 직접write 제거 확인) → #8이 HubSpot approved를 미러 예정. ④ #7c 수동 트리거 검증 OK(`{status:synced,onboarding:approved}`). **🐛 버그 2건**: (a) **credit_limit 파싱 버그** — 폼 Credit limit이 범위 드롭다운("$3,001 - $10,000")인데 #7a가 숫자만 추출해 "300110000"으로 깨짐 → #7a Validate&Normalize에 범위→상한값 매핑(0-3,000→3000 / 3,001-10,000→10000 / over→10001) 패치 + 이번 행 10000 보정. (b) forward-guard가 `hubspot_id_disputed`(이메일 오타 contact `sam.lim1747`≠폼 `sam.lim11747`) — **정상 작동**(버그 아님), disputed라 #7a의 HubSpot form submitted PATCH만 skip(승인엔 무관). **Tally 폼**: 보이는 Email에 정적 defaultAnswer `sam.lim11747@gmail.com` 박음(테스트용·편집가능) — **운영 전 hidden email_prefill 파이핑으로 전환 필요**. **미설정(사용자 UI)**: Airtable Automation 2건(#7b form status=approved / #7c form status 변경) 아직 안 만듦 — 지금은 수동 트리거로 대체. **다음**: 4단계(오더 #10 출하완료→#2 Credit-7days 인보이스 due+7) / 5단계(결제→#3). **Kumda 테스트데이터**: HubSpot 266849342928·contact 326266463686, 고객 recE6GawkFvfwREK3, 오더 #10 recV89PmcWyVebFoL, staging recSG3xYTfjyatDs5, Xero dbddc1ac. **추가 수정(같은 세션)**: ① 폼 entity 2종(상단 Entity name optional / Credit시 Legal entity name required) 중 후자(legal_entity)를 #7a가 파싱만 하고 저장 안 하던 버그 → staging `legal_entity` 필드 신설(`fldyMGJdZXcTYV0HX`) + #7a Prepare Body 저장 + #7b Safety Checks가 Xero Contact Name에 legal_entity>entity_name>shop_name 우선 사용. ② **#7a가 sales_pipeline 미반영 + onboarding=form submitted가 forward-guard 통과 시에만 PATCH**되던 문제 → #7a HubSpot PATCH에 `sales_pipeline:'onboard'` 추가 + IF Should PATCH 게이트에서 forward_guard_flag 조건 제거(회사 매칭만 되면 disputed도 반영). Kumda는 이미 approved라 재실행 안 하고 sales_pipeline=onboard 수동 set(#8 미러 확인용). ③ ABN auto-lookup: Tally 네이티브 불가 → 향후 n8n에서 ABR ABN Lookup API enrich(무료 GUID 필요) 옵션. #7a·#7b validate valid.

**2026-05-31 (회사) — 고객 여정 E2E 재개(Kumda Cafe) + 온보딩 status 모델 v3 + 명칭 통일**: 새 테스트 고객 **Kumda Cafe**(HubSpot `266849342928`) 생성 → #0 polling 33초 만에 Airtable 고객행 `recE6GawkFvfwREK3` + 매직토큰 `ea0e5d17-3cc7-4e48-ae3e-6fe83d00ca3d` + customer_group 일반고객 + HubSpot magic_token 역동기 전부 라이브 검증 ✅ (1단계). **명칭 통일 확정**: global link→Guest page / **magic link·magic token→Customer page**(magic 유지=churn 0). HubSpot 이메일 템플릿 2종은 사용자가 세팅(Global=`order.youngfoods.com.au`, Customer=`...?token={{company.magic_token}}`). **온보딩 status 모델 v3**: 고객 table `Onboarding Status`=HubSpot read-only 미러(#8, sample/pipeline과 동일)·사람 직접수정 X. 출처: form sent=HubSpot(영업)/form submitted=#7a 자동/pending information·approved=Airtable Submissions `form status`(admin). **구현 완료**: ① Airtable Onboarding Submissions `status`→**`form status`** rename(fldYSwtFPcy9a8eee, update_field는 옵션추가 미지원→typecast/UI로 form submitted·pending information 생성). ② #7a Prepare Airtable Body `status:'under review'`→`'form status':'form submitted'` + Slack 문구 갱신. ③ #7b Update Customer Row의 `Onboarding Status:approved` 직접write 제거(→#8 위임) + Update Staging Row `status:'propagated'` 제거(멱등=Xero ContactID 가드) + Safety Checks `fields.status`→`fields['form status']`. ④ #8 Search 노드 stale note 수정(approved 포함 미러가 정답). ⑤ **#7c 신설·active**(id `HyAA29msWiAHIxkz`, 8노드, valid): webhook `onboarding-formstatus-sync-v1` → staging fetch → form status가 valid 3종 & hubspot_company_id 있으면 HubSpot onboarding PATCH(generic) → #8이 고객 table 미러. #7a·#7b·#7c 모두 validate valid. **남은 사용자 UI**: Airtable Automation 신설(`form status` 변경 → #7c webhook {submission_id}) + 기존 #7b automation 트리거를 `form status=approved`로(필드명 rename 반영) + form status 옵션에 form submitted·pending information 추가. **다음**: 2단계(Kumda 매직링크 주문→#1 pre-onboarding hold) → 3단계(Tally 온보딩 폼→#7a→form status approved→#7b+#7c). **Kumda 테스트 데이터(정리대상)**: HubSpot `266849342928`, 고객 `recE6GawkFvfwREK3`.

**2026-05-31 — 고객 선호 언어 + 이중 언어 이메일 (라이브 검증)**: 외국인(특히 아시아) 비중 높아 다국어 대응. **이중 언어 = 영어 본문 항상 + 고객 선호≠en이면 `<hr>` 아래 모국어 블록** (담당자 교체 대비 영어 기본 + 영어 약한 고객 모국어 병기). 6개 코드 en/ko/zh/ja/th/es(주문사이트와 동일). 구현: ① Airtable 고객 `선호 언어`(fld5sLTgfjSbr3uol, default en)·staging `preferred_language`(fldS8yb3LFbVrN0kI) 신설. ② Tally `Me75K8` 섹션1에 "Preferred language for our emails" dropdown(English가 첫 옵션) 추가·발행. ③ #7a Validate&Normalize가 라벨→코드 매핑(한국/korea→ko 등, 기본 en) + Prepare Airtable Body가 staging 저장. ④ #7b Safety Checks에 preferred_language 전달 + Update Customer Row가 고객행 `선호 언어` PATCH + **Build Welcome Email 이중언어 재작성**(T[code] 템플릿맵 6종, payMap 영어 라벨). ⑤ #6c Build Recovery 동일 이중언어 재작성(고객행 `선호 언어` 읽음). ⑥ #R resolve Format Response가 `preferred_language` 반환. ⑦ #R2 Validate/Build Patch가 `language` 수집·검증·PATCH. ⑧ order-site: 수정 패널에 선호언어 `<select>` + read-mode 표시 + /api/update-contact가 language 전달 + globals.css select 스타일. Vercel prod 배포. **검증**: #6c를 ko로 트리거→영어+한국어 메일 발송(Gmail SENT) ✅, 수정 패널에서 zh 저장→Airtable 반영 ✅. **n8n validator false-positive 주의**: code 노드 헬퍼의 `return [...]`(문자열 배열)를 노드출력으로 오인 → `var parts=[...]; return parts.join()` 패턴으로 우회. **워크플로우 4개 valid**: #7a·#7b·#6c·#R·#R2. 미래 오더확인 이메일/SMS도 같은 T[code] 패턴 재사용.

**2026-05-30 — 전체 생애주기 E2E 완주 (James Bond Bakery, ①→③ 라이브)**: 신규 테스트 회사 "James Bond Bakery"(HubSpot `266762426813`)로 처음부터 끝까지 실측. ① HubSpot Company 생성 → #0 polling(5min) → Airtable 고객행 `recsDxSnSr6r3K3iV` + 매직토큰 `efd23479-...` + customer_group default 일반고객 + HubSpot magic_token 동기 ✅. ② 매직링크로 오더사이트(localhost:3000) 진입 → 샵배너 "James Bond Bakery" resolve(#R) → KAT 2박스 주문 → #1 → 오더 ORD-5 `rec8oMYbeqJhzpT64` + 라인 + pre-onboarding hold ✅. ③ 실제 Tally 폼 제출 → #7a staging → admin 승인(status=approved) → #7b → Xero Contact `JBB Pty Ltd`(`0b5e5ae7-...`) 생성 + 고객행 채움(COD·배송지·이메일) + HubSpot onboarding=approved + ORD-5 hold release + 환영이메일(Gmail) ✅. **Company가 전 생애주기 수렴**: magic_token(#0)+customer_group(#0)+onboarding=approved(#7b). **🔧 인프라 이슈 해결**: Tally webhook이 **Test URL(`/webhook-test/...`)로 잘못 등록**돼 제출 6건 유실 → Production URL(`/webhook/tally-onboarding-intake-v1`)로 교체 + Tally **Pro 업그레이드**(webhook은 Pro 전용) 후 정상 전송. **🐛 #7a 버그 수정**: Validate & Normalize가 DROPDOWN/CHECKBOXES value(옵션 ID)를 그대로 저장 → `options[].text`로 변환하는 로직 추가(payment term이 ID 대신 정식값 "COD"/"Direct Debit"로 저장됨). **🐛 #7b 버그 수정**: Release Hold Orders가 `오더 Hold` 체크박스만 풀고 `출하 상태`는 Hold로 남김 → Prepare Release Body에 `출하 상태='접수'` 리셋 추가 + Search Hold Orders filter를 `OR({오더 Hold}, {출하 상태}="Hold")`로 확장(#1은 pre-onboarding을 출하상태=Hold로도 표시). **테스트 데이터 추가(정리 대상)**: HubSpot `266762426813`, 고객 `recsDxSnSr6r3K3iV`, 오더 ORD-5 `rec8oMYbeqJhzpT64`, staging `recu1LuZKZYAzu35h`, Xero contact `0b5e5ae7-...`. 검증된 WF: #0·#R·#1·#7a·#7b 라이브 완주.

**2026-05-30 — 상태 모델 정립 + payment term 4종 재편 + #8 신설**: 사용자 지시로 (1) **고객 table에 3개 상태 필드 신설** — `Sales Pipeline`(fldwgR67iqR0iRnLB)·`Sample Status`(fldy2Q7cFskF6W1IB)·`Onboarding Status`(fldzrpO4pw7sOLnGU). HubSpot Company의 `sales_pipeline`/`sample`/`onboarding` property 미러. Onboarding Status 옵션은 HubSpot과 동일(form sent/submitted/reviewed/pending information/approved), Sales Pipeline·Sample Status는 보기 라벨 유지(API로 rename 불가) + #8이 HubSpot 소문자값→Airtable 라벨 매핑. (2) **고객 table에 회계 3필드 신설** — `회계 담당자`(fld6DO6NluI5i43yF)·`회계 이메일`(flddXJUiw0f22OUq7)·`회계 연락처`(fldsHIt6FrJCjs0VF). 기존엔 account 정보가 고객 table에 없었음. (3) **payment term 4종 확정**: `COD`/`DD`/`Credit - 7 days`/`Credit - EOM` (기존 Credit+credit_term 2필드 → 단일 4종으로). #7a가 Tally Payments→4종 매핑(`Credit - 7 days`/`Credit - EOM` 직접 산출), #7b validPaymentTerms 4종+is_credit, #2 due date 분기(DD=다음화/7days=+7/EOM=월말), #2.5 COD only. (4) **#7b가 approve 시 고객 table에 account 3필드 + Onboarding Status=approved 기록** 추가. (5) **#8 신설**(`WCf5ZNO0cP8PoV8v`, inactive, 5노드, valid): HubSpot Company search(20분 lookback, onboarding+sales_pipeline+sample) → Airtable 고객행 upsert(merge on HubSpot 고객 ID, 소문자→라벨 매핑). 영업이 HubSpot에서 단계 바꾸면 10분 내 Airtable 반영. **Onboarding Submissions에 `manager_name`(fldT41rALvWXIFsyY) 추가** + #7a가 저장.
**🖐 사용자 UI 손작업 (API 불가)**: ① 고객 table `Payment term` 옛 옵션 4개(GoCardless DD/7-day credit/Prepay/COD bank transfer) + staging `payment_term_selection` UUID 2개 삭제 — 새 4종은 typecast로 생성됨. ② 오더 table `Payment term`도 동일 정리. ③ **Tally 폼 Payments dropdown을 4옵션(COD/DD/Credit - 7 days/Credit - EOM)으로 재구성** — DD는 완료 후 GoCardless redirect, Credit 2종은 entity name·ABN/ACN·credit limit 분기. ④ Sales Pipeline·Sample Status 라벨을 HubSpot 소문자에 맞추고 싶으면 UI에서(선택 — #8 매핑이 처리하므로 불필요). **#8 활성화는 사용자 토글.** **settings.local.json 삭제 완료.**

**2026-05-30 (저녁) — payment term 3종 단순화 확정 (EOM·credit_term 폐기)**: 사용자 결정 = EOM 안 씀, Credit은 7 days로만 통일(단순·cash flow). **최종 3종 = `Direct Debit (default)` / `Credit - 7 days` / `COD`** (Tally 폼·고객 table Payment term·staging payment_term_selection 모두 이 라벨로 통일, 사용자가 UI 세팅 완료). `credit_term` 필드는 고객·staging 양쪽 삭제(사용자). 옛 payment term 옵션·staging UUID도 사용자가 UI에서 삭제 완료. **워크플로우 일괄 정합**: #7a payment 매핑 3종(credit_term 산출·저장 제거), #7b validPaymentTerms 3종+Safety Checks 통째 재작성(앞선 patch로 생긴 `customerGroup` 중복선언·`hProps`/`customerRecord` undefined SyntaxError 제거)·Update Customer의 is_credit 분기에서 credit_term 제거(credit_limit만), #2 validPayment=['Direct Debit (default)','Credit - 7 days']+due date 분기(DD=다음화/7days=+7, EOM 제거), #2.5 COD only 유지. 3개 WF validate valid. **정리 완료(2026-05-30)**: #2 Build Invoice 미사용 creditTerm 변수 제거 + #7b Build Welcome Email paymentTermLabelMap을 새 3종 라벨(Direct Debit (default)/Credit - 7 days/COD)로 교체. **#8 활성화됨(사용자 토글). 현재 active WF: #0·#1·#2·#2.5·#5·#7a·#7b·#8·#R (#3·#3-poll inactive).**

**현재 phase = 운영 액션 집행 중 + 온보딩 폼 v2 + #7a 빌드 (2026-05-29 저녁)**. **n8n 워크플로우 #0·#1·#7a skeleton 빌드 완료** (#0 활성화됨 / #1·#7a inactive). **온보딩 폼 v2 publish 완료**: Tally `Me75K8` 단일 page, GoCardless EMBED 제거, COD 옵션 추가, Hidden field (hubspot_id·email_prefill), Legal entity·ABN 신규. **GoCardless 연결 패턴**: Tally redirect on completion + 별도 redirect page에서 동적 BRT 생성 (별도 작업). 자세한 진행상황은 아래 "운영 액션 진행 현황" 섹션.

**2026-05-30 (집/맥북) — 앞단부터 E2E 구현·테스트 시작 (trial-and-error로 워크플로우 개선)**: 사용자 방침 = 골격 다 잡았으니 앞단부터 실제 구현하며 실행시켜 보고 워크플로우·프로세스 다듬기. **오더사이트 = Next.js+Vercel** 확정, `order-site/`에 빌드(App Router, 매직링크/게스트, 3 SKU 폼, 카트 MOQ/배송비, 멱등키, `/api/order`가 #1 webhook을 server-side 프록시 — 토큰 .env.local). **E2E 실측 결과**: ① HubSpot 테스트 Company 생성 → #0 polling 2분 만에 Airtable 고객행+매직토큰 자동 생성 ✅ 라이브 검증. ③+④ 오더사이트 → #1 → Airtable: KAT 2박스 주문 → 오더 `recZKaJso26a9vojg`(주문번호 1) + 라인아이템 생성, pre-onboarding 자동 hold ON ✅, 멱등성(duplicate_idempotent)·MOQ 거부($78<$150) ✅. **🔑 발견한 systematic 버그 (첫 runtime 실행으로 드러남)**: #1의 여러 노드가 이전 노드 참조에 `$('Node').json`(undefined!)·`$json`(중간 노드로 바뀜)을 써서 실패 → **`$('Node').first().json`으로 통일**해야 정상. 수정 노드: Compute & MOQ, Prepare Order, Prepare Line Items, Create Order(`$json.order_fields`→`$('Prepare Order').first().json`), Search Products, IF Needs Admin Alert, Slack Hold Alert, Respond Success. **⚠️ 같은 패턴 버그가 #2·#2.5·#3·#5·#7a·#7b에도 있을 가능성 큼** (다 skeleton, runtime 미실행) → 각 테스트 시 `$('Node').json`/`$json` 교차참조 점검 필요. **#1 활성화됨** (사용자 토글), order-intake-token credential 값 = 사용자가 n8n UI에서 설정(.env.local과 일치). **테스트 데이터(정리 대상)**: HubSpot Company `266779168230`, Airtable 고객 `rec0JzwOOQqexwXKc`(매직토큰 6ff160d7-…), 오더 `recZKaJso26a9vojg`+라인아이템 `recjanpFXlJf9f4oV`. 남은: ② 온보딩 폼 흐름(#7a→staging→#7b) 테스트, 오더사이트 고객정보 배너(resolve), Vercel 배포.

**2026-05-30 자리비움 스냅샷 (resume용, 갱신)**: 메모리 전부 commit (이번 세션 5건: 61be5d7→1b4edb6). n8n 워크플로우는 클라우드에 직접 저장됨(git 아님). **active n8n WF (13개)**: #0·#1·#2·#2.5·#3·#3-poll·#5·#7a·#7b·#8·#R + Platter Order Intake·_test_xero_credential. ⚠️ **#3·#3-poll도 active 상태로 남음**(결제→hold 테스트 때 켬, #3-poll은 5min 폴링 — 의도 확인 필요). **로컬 dev 서버**(order-site) 휘발성 — 재기동: `npm run dev --prefix /Users/limsungmin/yflifecycle/order-site --cache /tmp/yf-npm-cache` (포트 3000; 띄우기 전 `pkill -f "next dev"`). `.env.local`(gitignored) 그대로. **테스트 매직링크**: `http://localhost:3000/?token=6ff160d7-8560-44a5-baa6-70645e9e2ff9`(한밭) / `efd23479-5550-404d-bffa-6d59d080ef90`(James Bond Bakery). **다음 픽업 후보**: ① Tally 폼 3옵션 재구성(DD→GoCardless redirect, Credit 분기) — 사용자 UI ② James Bond Bakery로 3종 payment term E2E 재검증 ③ 테스트 데이터 정리(한밭·James Bond·#0 테스트 Company들) ④ 오더사이트 Vercel 배포 ⑤ #6(ClickSend)·#8(DD redirect handler). **테스트 데이터 잔존**: HubSpot Company 266779168230·266762426813·266741551606·266570728922, Airtable 고객/오더/staging 다수, Xero contact 2(한밭 8d4c19c7·JBB 0b5e5ae7)·invoice 2(INV-0001 PAID·INV-0002).

**2026-05-30 — #5 재무→HubSpot 동기 E2E 검증 ✅**: 고객 rec0JzwOOQqexwXKc에 hold=true·hold_reason·outstanding=395.5 세팅 → #5 트리거 → HubSpot Company 266779168230에 `account_hold=true`·`hold_reason`·`outstanding_aud=395.5` 정확 반영(멱등 needs_update 동작). **Company 레코드가 전체 생애주기 수렴 확인**: magic_token(#0)+customer_group(#0)+onboarding=approved(#7b)+재무 3필드(#5). **빌드된 워크플로우 검증 현황: #0·#1·#7a·#7b·#2·#2.5·#5·#3 전부 라이브 ✅ / #6 미빌드(ClickSend 대기).** (#3 검증 상세는 아래 2026-05-30 #3 항목.)

**2026-05-30 — #3 결제→Hold 동기 E2E 검증 ✅ (은행계정 블로커 해소)**: 사용자가 Xero에 BANK 계정 추가(`Young Foods #0718 Everyday`, id `e7655a9c-b266-418f-b04d-021757f50aa9`). 테스트: ORD-1(`recZKaJso26a9vojg`) Payment term→COD + INV-0001(`9e5ea419`, $161.50) 결제 기록(payment `da004b3d`) → Xero PAID → #3 활성화 → INVOICE.UPDATE webhook 트리거 → **최종 정확 동작**: 오더 결제상태 미결제→**Paid**·오더 Hold true→**false**(COD release) / 고객 outstanding 395.5→**239.5**(INV-0002만 잔존)·고객 Hold true→**false**·hold reason 공백. **🐛 런타임 버그 2건 발견·수정**: ① Parse Webhook Payload가 `$input.first().json.body`(=직전 Verify HMAC 출력 `{valid:true}`, body 없음) 읽어 항상 `no_events` skip → `$('Webhook (Xero INVOICE.UPDATE)').first().json` 직접 참조로 수정(#7b·#5 동일 `$('Node').first().json` 버그군 — 중간 노드 끼면 `$input`/`$json`이 엉뚱한 노드 가리킴). ② **Compute Customer Hold의 overdue 오탐**: Xero raw API의 `DueDate`는 `/Date(ms+0000)/` MS포맷(ISO는 별도 `DueDateString`) → `inv.DueDate.slice(0,10)="/Date(1748" < today`는 `/`<`2`라 항상 true → 모든 미결제 인보이스 연체 오탐 → `DueDateString` 우선 + `/Date()/` ms파싱 fallback으로 수정. **→ Xero 날짜 읽는 다른 노드도 같은 함정 주의(DueDate/Date 대신 DueDateString/DateString).** HMAC stub(valid) 유지. `$env`제거·tenant헤더는 이미 반영돼 있었음.
**빌드된 워크플로우 검증 현황: #0·#1·#7a·#7b·#2·#2.5·#5·#3 전부 라이브 ✅ / #3-poll inactive(코드준비, 같은 DueDate함정·Parse버그 점검 필요) / #6 미빌드(ClickSend 대기).**

**2026-05-30 — #2 dispatch 인보이스 E2E 검증 ✅**: 고객 rec0JzwOOQqexwXKc를 Credit/EOM으로 설정 → #1로 새 주문(order_no 2, `rec8I882m4p8Oglhq`, KAT×3 $234) → 출하완료로 변경 → #2 트리거 → **Xero 인보이스 `fe39c182-329a-45d4-95fa-a29fe2a6425e` 생성, due_date 2026-05-31(월말=EOM 로직 정확)**, Airtable 결제 상태 미결제·hold 없음. **#2 한 번에 3개 수정 동시 검증**: ① Credit/EOM due 로직(신규) ② Fetch Line Items RECORD_ID 필터(ARRAYJOIN 버그 수정) ③ 배송비 AccountCode '200'. **재무 발행 양쪽(#2 dispatch + #2.5 COD) 모두 라이브 검증 완료.** **#1 Search Duplicate Orders ARRAYJOIN 버그는 아직 잔존**(중복 의심 미탐지, blocking 아님). 테스트 데이터 추가: 오더 `rec8I882m4p8Oglhq`(order_no 2) + Xero invoice `fe39c182-…`.

**2026-05-30 — Payment term 택소노미 확정·전 시스템 적용 ✅**: 정식값 = **`Direct Debit`(default·GoCardless mandate, #2 dispatch 인보이스, due 다음 화요일) / `Credit`(+companion `credit_term` 7 Days|EOM + `credit_limit`; #2 dispatch 인보이스, due 7일|월말; #3 credit_limit·overdue hold) / `COD`(게스트·1회성, #2.5 주문 시 인보이스 + hold, 출하 전 결제: 수동입금 or Xero Stripe pay-now)**. 적용: Airtable 고객/오더 single-select(typecast로 정식값 생성) + 신규 필드 고객 `credit_term`(fldoWnDAIdCxorpqH)·staging `credit_term`(fldL3iEwXGB60CTLG)/`credit_limit`(fldR1bRoi9tnDsDOF) / #7a 폼라벨→정식값 매핑+credit staging 저장 / #7b 정식값+Credit시 credit_term·limit 고객 저장 / #2 validPayment=[Direct Debit,Credit]+due 로직 / #2.5 validPayment=[COD]. 4개 WF validate valid:true. **잔여**: ① Airtable 옛 옵션 4개(GoCardless DD/7-day credit/Prepay/COD bank transfer) 제거 — 사용자 UI(update_field가 single-select 옵션 변경 미지원). ② Credit 경로(#2 7일/EOM) live 테스트는 #2 실측 시. ③ COD=게스트 흐름: 게스트가 Xero contact 받는 절차 미정(소규모 flow 결정 필요). ④ Xero 인보이스 Stripe pay-now 활성화(사용자 Xero 설정).

**2026-05-30 — #2.5 인보이스 발행 E2E 검증 ✅**: 테스트 오더 `recZKaJso26a9vojg`로 #2.5 트리거 → **Xero 인보이스 생성 성공** (InvoiceID `9e5ea419-9eed-43db-b5e1-881b8baf22e3`, 총 $161.50 = 제품 $156 EXEMPTOUTPUT + 배송 $5 OUTPUT + GST $0.50) + Airtable 오더에 Xero 인보이스 ID·오더 Hold=true·미결제 반영. **발견·수정 버그**: (a) 배송비 라인에 `AccountCode` 누락 → Xero "Account code or ID must be specified" → #2·#2.5 Build Invoice Body 배송비 라인에 `AccountCode: '200'` 추가(제품 라인은 ItemCode가 계정 자동결정). (b) **Payment term 택소노미 불일치** — 고객/폼은 `COD (bank transfer before dispatch)`인데 #2.5는 `['Prepay','COD']` 기대 → #2.5에 COD 라벨 추가(band-aid). **#2는 `GoCardless DD`/`7-day credit` 기대 — 새 3종(Direct debit/Credit application/COD)과 불일치. #1·order-site·Airtable single-select 옵션까지 걸친 전 시스템 택소노미 reconciliation 필요(미결 결정).** (c) #2.5 Fetch Line Items는 RECORD_ID 기반으로 수정 완료 / **#2 Fetch Line Items는 아직 ARRAYJOIN 버그 — #2 실측 시 동일 수정 필요**. **테스트 데이터 추가**: Xero invoice `9e5ea419-…`.

**2026-05-30 — ② 온보딩 흐름 E2E + 추가 버그 발견 (#7a→staging→#7b)**: #7a 트리거(Tally 시뮬레이션, hidden hubspot_id=266779168230) → staging `recamSyuAL1ykMJSq` 생성 ✅ (matched_via=hubspot_id_disputed — 회사에 연결 contact 없어 forward-guard 검증 불가, 정상). status=approved → #7b 트리거 → 끝까지 완료 ✅, **pre-onboarding hold 오더(recZKaJso26a9vojg) 자동 release ✅**(오더 Hold true→false), HubSpot onboarding=approved, 고객행 update(payment term COD·이메일·배송지), staging→propagated, 환영 이메일 노드 통과. **발견·수정한 버그(런타임에서만 드러남)**: (a) **n8n Code 노드 `$env` 접근 차단**(n8n Cloud, "access to env vars denied") → #7b Build Welcome Email의 `$env.ORDER_SITE_BASE_URL` 하드코딩으로 수정. **⚠️ #3 Verify HMAC·#3-poll Extract&Sign도 `$env.XERO_WEBHOOK_KEY/$env.XERO_TENANT_ID` 사용 → 같은 실패 예정, credential/static data로 바꿔야 함.** (b) **Airtable 링크 record를 ID로 매칭 시 `FIND(id, ARRAYJOIN({링크}))`는 버그**(ARRAYJOIN은 이름 반환) → #7b Search Hold Orders를 고객의 링크된 오더 record ID + `RECORD_ID()` 기반으로 수정. **⚠️ #1 Search Duplicate Orders에 같은 버그 잠복.** (c) #7a Code 노드 4개 `.item.json`→`.first().json` 선제 수정(runOnceForAllItems에서 `.item` 불안정). (d) Xero PaymentTerms.Type `DueOnReceipt`/`NetOfDate`는 무효 enum(DAYSAFTERBILLDATE 등만) → #7b contact 생성에서 PaymentTerms 제거. **🔴 CRITICAL 블로커 확정**: **n8n `xero-custom-connection`(98R0oS6cSE9DtxYP)으로 Xero Contact 생성 실패** — 동일 payload를 Claude의 Xero MCP 연결로는 즉시 성공(ContactID 8d4c19c7-f580-42c1-a529-0a99381c85a8 생성됨). 즉 n8n Xero 연결의 **scope 부족 또는 연결 손상**(메모리의 "Xero scope 문제 진단 대기"가 실제 블로커로 확정). **이 연결을 #2·#2.5·#3가 공유 → Xero 쓰기/읽기 전부 영향.** **✅ 해결 (2026-05-30)**: 원인 = ① n8n credential clientSecret이 placeholder였음(실제값 입력) + ② scope에 `accounting.transactions`를 요청했는데 이 Custom Connection엔 그 umbrella scope가 없음(세분화 scope) → `accounting.invoices`로 교체. 최종 n8n Scope = `accounting.contacts accounting.invoices accounting.settings`. `_test_xero_credential`로 Items 3종 반환 검증 완료. **잔여 → 완료(2026-05-30)**: #2·#2.5·#3·#3-poll Xero HTTP 노드에 `Xero-tenant-id` 헤더 전부 추가 완료. **새로 발견된 잠복 버그**: #2·#2.5 `Fetch Line Items` 필터가 `FIND(record_id, ARRAYJOIN({오더}))` — {오더} ARRAYJOIN은 주문번호(autonumber)를 반환하지 record_id가 아님 → line items 0건 매칭 실패 예정(#1 Search Duplicate, #7b Search Hold와 동일 ARRAYJOIN 버그군). #2/#2.5 실측 시 RECORD_ID 기반 또는 오더의 라인아이템 링크 기반으로 수정 필요. (e) 마이너: #7b Release Hold Orders가 `오더 Hold` 체크박스만 풀고 `출하 상태`는 "Hold"로 남김 → `접수`로 리셋 보강 권장. **테스트 데이터(정리 대상)**: HubSpot Company `266779168230`, Airtable 고객 `rec0JzwOOQqexwXKc`, 오더 `recZKaJso26a9vojg`+라인 `recjanpFXlJf9f4oV`, staging `recamSyuAL1ykMJSq`, Xero contact `8d4c19c7-…`. **#1·#7a·#7b 모두 active 상태로 남음** (테스트 후 정리/재검토 필요).

**2026-05-30 (집/맥북) — "운영 액션 마무리" A트랙 4건 완료**: ① **Airtable 고객 `credit_limit` 필드 신설** (`fldmZiwCKyybPcSCI`, currency) — #3 Compute Customer Hold가 이미 읽도록 작성돼 있어 필드만 추가하니 `outstanding ≥ credit_limit` 로직 활성화. ② **#3-polling fallback 워크플로우 신설** (`atQNwPt7B3QUBLBi`, 5노드, inactive, valid) — Schedule :05 → Xero If-Modified-Since(now-65m) → 변경 인보이스를 #3 webhook 재투입(HMAC 동일키 서명). `n8n/workflow-3-poll-skeleton.json`. ③ **#7b Phase 3 환영 이메일 완료** (#7b 22→**26노드**) — **Gmail OAuth2 발송**(cred `SycEHwXNU8mv9tYf`) + **HubSpot Company 타임라인 email engagement 로깅**(POST /crm/v3/objects/emails, email→company typeId 186, CRM 활동 로그 원칙). released_count 조건부 wording. 매직링크=env ORDER_SITE_BASE_URL+'/?token='+토큰. ④ **#1 컷오프 timezone DST-aware** — Validate Input의 02:00 UTC 하드코딩 → Intl.DateTimeFormat('Australia/Sydney') 이중패스로 전날 12pm Sydney 정확 계산. 문서 갱신: n8n/CLAUDE.md(#7b step12·#3 트리거·credit_limit), airtable/schema.md.

설계 닫힌 영역: order-site UX, 가격/할인/배송비/DD, 온보딩 폼, **n8n 워크플로우 #1~#7 전부 (#2.5·#5 포함)**, **MOQ($150 grand subtotal), 배송비($5+GST, OUTPUT 10%), Xero Item Code(KAT/GAR/TER), Slack ops-* 매핑** (2026-05-28 추가 closure). #2.5는 Airtable trigger + payment term 필터 패턴(#1 결합 X), 결제 채널 = manual bank transfer, COD는 "주문 후 빠른 선결제 변형". **#5 (재무→HubSpot 공유)**: Airtable 고객 change trigger → HubSpot 3-property(Account Hold·Hold Reason·Outstanding) PATCH, 멱등 비교 skip, 확장 항목(overdue·credit limit·last payment)은 운영 시작 후 보강. `xero/` 도메인 미결 모두 해소. **`airtable/production-planning.md` v1 작성 + `schema.md`에 Production Plan/Schedule 2 테이블 + 제품 lookup 3필드 반영** (2026-05-28 추가). #3는 INVOICE.UPDATE webhook + 매시간 polling, #4(credit limit) #3에 통합. **온보딩 폼 #7 a/b 분할 설계 완료 (2026-05-28)**: #7a Tally webhook → 매칭(+forward 가드) → Airtable `Onboarding Submissions` staging → HubSpot Onboarding=form submitted → Slack. 영업 review에서 `Customer Group`(신설 HubSpot Company property) 지정 + Onboarding=approved. #7b HubSpot workflow webhook → Xero Contact 생성(discount % 그룹 기반) → Airtable 고객 행 + 토큰 발급 → 미배정 소급매칭 후보 Slack → 환영 이메일. 영업·고객·어드민 단일 진입 패턴 유지.

라벨·박스 단위 갱신 반영 완료: KARAAGE·TERIYAKI 라벨(=bag) 2.5kg→4kg + **박스 = 2 bag = 8kg 확정**, 박스 단가 $32.50→$52→**$104** ([[product-source-files]]).
Xero 환경 확보 + Item 3개 등록 완료 ($78/$104/$104, EXEMPTOUTPUT) ([[xero-account]]).

## 운영 액션 진행 현황 (2026-05-28 밤 기준)

### ✅ 완료 (Claude가 MCP로 진행)
1. Airtable `yflifecycle` base에 운영 테이블 **8개 신설** — 고객·제품·오더·라인아이템·Production Schedule·Production Plan·Onboarding Submissions·SMS Log. 모든 tableId·필드 ID·formula/lookup UI 작업 목록은 [[airtable-base]] 참조.
2. HubSpot Company custom property **3개 신설** (companyinformation 그룹):
   - `customer_group` (enumeration: 내부고객/일반고객) — #7b가 Xero discount % 산정 source
   - `hold_reason` (text) — #5가 동기
   - `outstanding_aud` (number, AUD) — #5가 동기
3. **ClickSend 계정 + API key 발급 + 알파태그 `YoungFoods` 신청** (2026-05-29 아침, 사용자). 승인 ~24h 대기 중 (목표 2026-05-30).
4. **n8n `clicksend-creds` (httpBasicAuth) 등록 완료** — id `WZGjzqhfPeU4PL4i`, user = sungmin.lim@youngfoods.com.au, 도메인 제한 `rest.clicksend.com`. API key 본문은 n8n에만 보관 (git 메모리엔 미저장).
5. **schema.md + n8n/CLAUDE.md에 base/tableId 인라인 박기 완료** (2026-05-29). schema.md = top 표 + 각 섹션 헤딩 인라인. n8n/CLAUDE.md = "외부 리소스 ID 참조" 섹션에 Airtable + n8n credential ID 7개. 워크플로우 빌드 lookup 단일화.
6. **제품 3 SKU 행 Airtable 입력 완료** (2026-05-29). record ID: KAT `recKAV446g9hwsymE` ($78), GAR `recSpIZqWHvFkXM8l` ($104), TER `recBEjmZvvNB040Ao` ($104). 박스 단가 formula 검증 통과. lookup 3필드(target/safety/default_dispatch)는 미결 blank.
7. **n8n 워크플로우 #1 skeleton 빌드 완료 (+ Slack Hold Alert 추가 2026-05-29 저녁)**. id `twZw1wv3Fc1iJdeO`, **21 노드** (Slack Hold Alert + IF Needs Admin Alert 추가), **inactive**. Webhook(headerAuth=`order-intake-token` id `dY4EiGLUEsseUd4h`) → Validate Input → IF → Search Order Dedupe → IF → Search Products → Compute & MOQ → IF → Search Customer → Prepare Order → IF → Create Order → Prepare Line Items → Create Line Items → Respond Success. 4개 error response 노드(Invalid/Existing/MOQ/Token Invalid). Airtable는 HTTP Request 노드로 API 직접 호출(resourceMapper 회피). 워크플로우 JSON 원본은 `n8n/workflow-1-skeleton.json`. **stub/미구현**: 컷오프 검증, 중복 의심 Slack, **pre-onboarding/게스트 Slack 알림(다음 이터레이션 — 자동 hold 로직은 추가됨)**, SMS sub-workflow(#6a 미존재), 부분 실패 복구. n8n validate runtime profile: 0 errors / 16 warnings(false positive 위주).
8. **온보딩 모델 v2 개정 + #1 보강** (2026-05-29 오후). 매직 토큰 발급 시점 = HubSpot Company 생성 직후 (#0 신설 설계), 승인 주체 = admin team (영업 X), 승인 트리거 위치 = Airtable `Onboarding Submissions.status=approved` (HubSpot workflow → Airtable 자동화로 변경), 고객 그룹 지정 = HubSpot Company 생성 시 영업 (default 일반고객), pre-onboarding 주문 = derive only (필드 추가 X). HubSpot Company `magic_token` property 신설(MCP 완료). #1 Prepare Order 코드 갱신: 매직링크+payment term blank → 오더 hold ON 자동 + alert flag; 게스트도 동일. 문서 일괄 개정: 루트 CLAUDE.md, onboarding/CLAUDE.md, hubspot/customer-profile.md + sales-pipeline.md, n8n/CLAUDE.md(#0 detail + #7b 개정), airtable/schema.md.
9. **n8n 워크플로우 #0 skeleton 빌드 완료** (2026-05-29 오후). id `lNABcJuKPABoqQf0`, 6 노드, **inactive**, **옵션 B (polling 5min)** 채택. Schedule Trigger (5분) → HTTP Search HubSpot companies (createdate > now-10min) → Code Extract Companies (results array → N items) → Code Process Each (runOnceForEachItem, UUID v4 토큰 생성, payload 빌드) → HTTP Airtable Upsert (PATCH `performUpsert.fieldsToMergeOn = HubSpot 고객 ID`) → HTTP HubSpot PATCH magic_token (customer_group default 보완). 멱등 = HubSpot 고객 ID upsert + 기존 magic_token 보존. 워크플로우 JSON 원본 `n8n/workflow-0-skeleton.json`. **주의/limitations**: ① HubSpot Private App credential 이름이 "(companies read)" — Company PATCH 실패 시 scope `crm.objects.companies.write` 추가 필요. ② createdate filter 기반이라 이미 존재하는 기존 Company는 처리 안 됨 (bootstrap 시 수동 fire 또는 임시 lookback 확장 필요). ③ 5min latency.

10. **#0 수동 테스트 + 멱등성 검증 + 활성화 완료** (2026-05-29 저녁). HubSpot scope `crm.objects.companies.write` 확인됨. 테스트 Company `266741551606` 생성 → manual exec 6 노드 모두 통과 → Airtable 행 `recLnAuI2bsBBtsI8` + 매직 토큰 `663bb76f-...` + HubSpot magic_token 양쪽 동기화. **멱등 재실행**: record id·createdTime·토큰·HubSpot lastmodifieddate 모두 변동 없음 (HubSpot이 동일 값 PATCH를 no-op 처리). Airtable 테스트 행 삭제 + 워크플로우 Activate 토글 ON (5min cron 시작). HubSpot 테스트 Company는 MCP delete 미지원 → 사용자가 UI에서 archive 필요.

11. **Tally 폼 Me75K8 v2 publish 완료** (2026-05-29 저녁). 변경: GoCardless EMBED + DD Request TITLE 제거, Hidden field 2개(`hubspot_id`, `email_prefill`) 추가, COD dropdown 옵션 추가, Credit Application 분기에 Legal entity / ABN/ACN 추가, conditional logic 갱신 (Credit Application 선택 시 4개 필드 표시). **GoCardless 모델 결정**: Tally 정적 임베드 불가 (BRT single-use) → Tally redirect on completion + 별도 redirect page에서 동적 BRT 생성 패턴. Redirect page 구현은 별도 작업. Tally signing secret 발급은 사용자 손작업 (Integrations > Webhooks).

19. **n8n 워크플로우 #5 skeleton 빌드 완료** (2026-05-29 밤). id `EM3FKoINCG3ytoNV`, **11 노드**, **inactive**, validate `valid: true`. **트리거 = Webhook** (path `customer-finance-sync-v1`, Airtable Automation 호출). Webhook → Validate → Fetch Customer → Extract HubSpot ID → IF Has HubSpot ID → [Fetch HubSpot Company(현 값) → Compute Diff(멱등 가드 — 현 값과 동일하면 skip) → IF Needs Update → [Patch HubSpot account_hold/hold_reason/outstanding_aud / skip] → Respond Success] / Respond Skipped Guest]. 게스트(HubSpot 고객 ID 없음) silently skip. hold=false면 hold_reason 강제 공백. JSON 원본 `n8n/workflow-5-skeleton.json` (lightweight pointer). **사용자 손작업**: Airtable Automation 설정 — 고객 테이블 {고객 Hold, hold reason, outstanding} 변경 시 → n8n webhook POST `https://youngfoods.app.n8n.cloud/webhook/customer-finance-sync-v1` with `{record_id}`.

24. **n8n 워크플로우 #3 Phase 1: HMAC 검증 추가** (2026-05-29 밤). 15→**18 노드**, validate `valid: true`. 새 노드 3개: Verify HMAC (Code, env `XERO_WEBHOOK_KEY` 있으면 SHA256 검증, 없으면 stub valid=true로 fallback), IF Valid Signature (IF), Respond Unauthorized (401). 흐름: Webhook → Verify HMAC → IF Valid → [true: Parse Webhook Payload → ... 기존 흐름 / false: Respond Unauthorized]. **사용자 손작업**: Xero developer console에서 webhook signing key 발급 → n8n 환경변수 `XERO_WEBHOOK_KEY` 설정 (또는 credential로 변환). 설정 전까지는 stub mode (valid=true)로 자동 통과 — 테스트 편의. **Phase 2 (Polling fallback) 보류**: trigger 2개 합류 복잡도 (Schedule trigger 추가 시 Parse Webhook Payload reference하는 노드들이 polling branch에서 fail) → 별도 워크플로우로 분리하는 게 깔끔. next iter 또는 별도 turn.

18. **n8n 워크플로우 #3 skeleton 빌드 완료** (2026-05-29 밤). id `YqkAw7AJKw5uPBpv`, **15 노드**, **inactive**, validate `valid: true`. **트리거 = Webhook** (path `xero-invoice-update-v1`, Xero INVOICE.UPDATE; polling fallback은 stub TODO). Webhook → Parse Webhook Payload → IF Should Process → [Fetch Xero Invoice → Find Airtable Order(filterByFormula by Xero 인보이스 ID) → Compute Order Update(멱등 가드: 현 값과 같으면 skip / Prepay·COD + Paid면 오더 Hold 해제) → IF Order Needs Update → [Update Order / skip] → Find Airtable Customer(filterByFormula by Xero ContactID) → Fetch AR Invoices(GET /Invoices?ContactIDs=&Statuses=AUTHORISED,SUBMITTED → outstanding 재계산) → Compute Customer Hold(overdue ≥1 OR outstanding ≥ credit_limit 시 hold ON, hold_reason 산출; 멱등 가드) → IF Customer Needs Update → [Update Customer / skip] → Respond Success] / Respond Skipped]. JSON 원본 `n8n/workflow-3-skeleton.json` (lightweight pointer). **stub/미구현 (다음 iter)**: ① HMAC-SHA256 검증 (Xero webhook key), ② polling fallback (매시간 :05) 별도 트리거, ③ Airtable 고객에 credit_limit 필드 (현재 없음 → overdue만으로 hold 결정), ④ 매칭 실패 Slack 알림. **사용자 손작업**: Xero developer console에서 webhook 등록 (URL `https://youngfoods.app.n8n.cloud/webhook/xero-invoice-update-v1`) + Xero webhook signing key 발급 → n8n credential `xero-webhook-key` 등록 + (옵션) Airtable 고객 테이블에 `credit_limit` (currency) 필드 추가.

17. **n8n 워크플로우 #2.5 skeleton 빌드 완료** (2026-05-29 밤). id `GuMEWpGQ4506RmHf`, **14 노드**, **inactive**, validate `valid: true`. **#2와 거의 동일 구조**, 차이: ① webhook path `prepay-cod-invoice-v1`, ② Safety Checks validPayment=`['Prepay', 'COD']` + dispatchStatus in `['접수', 'Hold']`, ③ Build Invoice Date=DueDate=today (DD/7-day 산정 로직 없음), ④ Update Airtable에 `오더 Hold=true` 추가. PDF Branding theme footer에 회사 계좌 표기 (사용자 손작업). 결제 확인은 #3가 INVOICE.UPDATE 수신 시 hold 해제. JSON 원본 `n8n/workflow-2-5-skeleton.json` (lightweight pointer, 본체는 n8n에서 fetch). **사용자 손작업**: Airtable Automation 설정 (오더 생성 + 출하 상태=접수 AND payment term ∈ {Prepay, COD} → n8n webhook POST `{record_id}`).

23. **Xero discount % 메커니즘 확정 + #2/#2.5 line DiscountRate 적용** (2026-05-29 밤). **확인 결과**: Xero Contact `Discount` field는 GET response에는 있지만 POST/PUT optional field list에 없음 → API로 Contact-level discount set **불가능**. Xero UI에서만 수동 설정 가능. **결정 = Option B (invoice line 명시)**: #2/#2.5 Build Invoice Body에서 customer_group 기반 line DiscountRate 매번 명시. 내부고객 → 제품 line DiscountRate=5, 일반고객 → 미설정(default 0). 배송비 line은 기존 DiscountRate=0 유지 (그룹 할인 제외). #7b Xero Create Contact body는 변경 X (Discount field 못 받음). **#2/#2.5 둘 다 valid: true**. **Why**: Contact UI 작업 의존 X, 매번 자동, robust. Xero docs scrape 결과 https://developer.xero.com/documentation/api/accounting/contacts POST optional field list 검증 완료.

16. **n8n 워크플로우 #2 skeleton 빌드 완료** (2026-05-29 밤). id `W1qYHJKHtPzKpbm4`, **14 노드**, **inactive**, validate `valid: true`. **트리거 = Airtable Automation webhook** (path `dispatch-invoice-v1`) — Airtable Trigger 노드의 triggerField가 lastModifiedTime을 요구하는데 MCP create_field 미지원 → 일관성+robust 위해 #7b처럼 webhook 채택. Webhook → Validate Payload → Fetch Order → Fetch Customer → Fetch Line Items(filterByFormula `FIND(record_id, ARRAYJOIN({오더}))`) → Safety Checks(dispatch 상태/payment term DD·7-day/Xero ContactID/멱등) → IF Pass Safety → Build Invoice Body(line items + 조건부 배송비 line($5+GST OUTPUT) + Due Date 산정[DD=다음 화요일 / 7-day=+7일]) → Xero POST /Invoices(LineAmountTypes=Exclusive) → Xero POST /Invoices/{id}/Email(PDF 자동 발송) → Update Airtable(Xero 인보이스 ID + 결제 상태=미결제) → Slack `#ops-accounts` 알림 → Respond Success / Aborted with skip_reason. JSON 원본 `n8n/workflow-2-skeleton.json`. **stub/미구현 (다음 iter)**: ① Xero Item Code·품목명 lookup을 라인아이템 제품 fetch로 강화 (현재는 label에서 sku 추출), ② 고객 그룹 discount % 자동 적용 메커니즘 확인 후 라인에 DiscountRate, ③ skip 분기 Slack 알림. **사용자 손작업**: Airtable Automation 설정 (오더.출하 상태=출하완료 AND payment term ∈ {GoCardless DD, 7-day credit} → n8n webhook POST body `{record_id: <record id>}`).

22. **n8n 워크플로우 #1 보강: 컷오프 검증 + 중복 의심 Slack** (2026-05-29 밤). 21→**25 노드**, validate `valid: false` (1 false-positive error). **Validate Input 코드 갱신**: `requested_delivery_date`가 ISO date 형식이면 cutoff (전날 12pm Sydney 시간 ~ 전날 02:00 UTC) 비교 후 초과 시 `cutoff_exceeded` push. **새 노드 4개**: Search Duplicate Orders (filterByFormula: customer_id 있으면 같은 고객+배송일 매칭, 게스트면 FALSE() → 0건 자동 skip), Duplicate Decision (Code, has_duplicate flag + Slack text 빌드), IF Has Duplicate, Slack Duplicate Alert (`#ops-orders` C0B3WQ6EPQW). 분기: IF Customer OK [true] → Search Duplicate Orders → ... → IF Has Duplicate → [Slack / skip] → Create Order. blocking 안 함 (admin 판단). **n8n validator false-positive**: Duplicate Decision Code 노드 "Cannot return primitive values directly" error — 외부 변수 추출 패턴 사용했음에도 트리거 (#7b Soft Match Decision은 동일 패턴인데 통과). runtime은 정상 작동 예상. **stub TODOs**: sku 조합 일치 검증 (현재는 customer+date 매칭만), timezone-aware cutoff (현재 UTC 기준 단순 계산).

14. **n8n 워크플로우 #1 보강: Slack Hold Alert 노드 추가** (2026-05-29 저녁). 21 노드. Create Line Items → IF Needs Admin Alert → [true: Slack Post Hold Alert(#ops-onboarding) → Respond Success / false: Respond Success]. Prepare Order 노드의 `needs_admin_alert`/`alert_reason` flag 활용. pre-onboarding 또는 게스트 주문 시 알림.

15. **n8n 워크플로우 #8 (DD Redirect handler) 설계 완료, 빌드 보류** (2026-05-29 저녁). n8n/CLAUDE.md에 명세 추가. Tally 폼 완료 → redirect URL 도착 → payment method 분기 → DD면 GoCardless API로 동적 BRT 생성 후 302 redirect, Credit/COD는 정적 HTML 응답. 빌드는 GoCardless credential 등록(사용자 손작업) 후 별도 turn.

20. **n8n 워크플로우 #7b 보강 Phase 1+2 완료** (2026-05-29 밤). id `pvvhhs5expWOfJce`, **22 노드** (13→22), validate `valid: true`. **Phase 1**: Update Customer Row → Search Hold Orders(filterByFormula 고객+Hold+미결제) → Prepare Release Body → IF Has Holds → [Release Hold Orders batch PATCH / skip] → HubSpot PATCH Onboarding=approved → Update Staging. **Phase 2**: HubSpot PATCH Onboarding Approved → Search Soft Match(filterByFormula 고객 빈 link + (상호==shop_name OR 배송지==delivery_address)) → Soft Match Decision(Slack 메시지 빌드, Code 노드의 inline object literal validator 까다로움 → 외부 변수 추출 패턴 발견) → IF Has Soft Match → [Slack Soft Match Alert / skip] → Update Staging. Slack Post Success 메시지에 released_count 정보 추가. **Phase 3 (stub TODO)**: 환영 이메일 — HubSpot single-send transactional API (Marketing Hub paid plan 필요 확인) 또는 Gmail OAuth2 직접 발송. 조건부 wording (released_count > 0 시 "기존 hold 오더 dispatch 진행, 다음 오더부터 매직 링크 사용" / == 0 시 일반 환영). **n8n Code 노드 validator 발견사항**: inline object literal `return [{json: {...}}]`은 "primitive values directly" error 트리거 — 외부 변수로 추출하면 통과 (다른 동일 패턴 노드는 통과해서 false positive로 의심하나 일관성 위해 외부 추출 권장).

13. **n8n 워크플로우 #7b skeleton 빌드 완료** (2026-05-29 저녁). id `pvvhhs5expWOfJce`, **13 노드**, **inactive**, validate `valid: true`. Webhook(path `tally-approved-propagate-v1`, Airtable Automation 트리거) → Validate Payload → Fetch Staging Row(by submission_id) → Fetch HubSpot Company(customer_group 등) → Fetch Customer Row(by HubSpot 고객 ID) → Safety Checks(staging found+approved, customer row found, payment_term valid, 멱등 — Xero ContactID 이미 있나) → IF Should Proceed → [Xero Create Contact(genericCredentialType oAuth2Api `xero-custom-connection`, PaymentTerms by group, TaxNumber=ABN) → Update Customer Row(Xero ContactID + 담당자/배송지/payment term/그룹) → Update Staging Row(status=propagated, linked_customer) → Slack Post Success(#ops-onboarding) → Respond Success / Respond Aborted with skip_reason]. JSON 원본 `n8n/workflow-7b-skeleton.json`. **stub/미구현 (다음 iter)**: ① Hold orders 자동 release, ② HubSpot Company Onboarding=approved sync, ③ 환영 이메일(조건부 wording — hold 오더 있으면 "다음 오더부터 이 링크 사용"), ④ 미배정 게스트 주문 소급매칭 후보 Slack, ⑤ Xero Contact discount % 자동 적용 메커니즘 확인 (현재는 Airtable 고객 그룹만 저장, #2가 invoice line에서 적용).

21. **n8n 워크플로우 #7a Contact email fallback 추가** (2026-05-29 밤). 24→**25 노드**, validate `valid: true`. **Matching 3-tier 완성**: Tier 1 hidden hubspot_id (Forward 가드 포함) / Tier 2 form_email로 HubSpot Contact 검색 → associatedcompanyid로 Company 추출 / Tier 3 unmatched. **새 노드**: HubSpot Search Contact by Email (POST /crm/v3/objects/contacts/search with email filter + associatedcompanyid property). **기존 Decide Match (unmatched) Code 갱신**: matched_via 결정 ('email' / 'email_no_company' / 'unmatched'). **Build Slack Message matchEmoji map 확장** ('email'·'email_no_company'). **IF Should PATCH HubSpot 조건 변경**: matched_via='hubspot_id' → `hubspot_company_id NOT EMPTY AND !forward_guard_flag` (email 매칭도 PATCH 가능).

12. **n8n 워크플로우 #7a skeleton 빌드 + 보강 완료** (2026-05-29 저녁). id `ylRJHUCFQ9RmCa6R`, **24 노드**, **inactive**, validate `valid: true`. Webhook(path `tally-onboarding-intake-v1`, responseNode) → Validate & Normalize → Search Existing Submission(멱등) → IF Already Exists → [respond already / continue] → IF Has HubSpot ID → [HubSpot Get Company → IF Company Found → [Get Associations → Extract Contact IDs → IF Has Contacts → [Batch Read Contacts → Forward Guard Check / No Contacts Set] / Set Invalid ID] / Decide Match (unmatched)] → Prepare Airtable Body(staging row fields + matched_via 통과) → Create Airtable Submission → Build Slack Message → Slack Post(`#ops-onboarding` C0B42FWL8VA) → Update Submission Slack ts(Airtable PATCH) → IF Should PATCH HubSpot(matched_via=hubspot_id AND !forward_guard_flag) → [HubSpot PATCH Onboarding=form submitted / skip] → Respond Success. **HubSpot Company `onboarding` property 확인됨** (enum: form sent / form submitted / form reviewed / pending information / approved). JSON 원본 `n8n/workflow-7a-skeleton.json`. **stub/미구현 (다음 iter)**: ① HMAC 검증 (Tally signing secret 후), ② Contact search by email fallback (hubspot_id 없을 때), ③ gocardless_mandate_id 저장 (DD redirect 페이지 구현 후).

### ⏳ 남음 (사용자 UI 작업) — **`GO-LIVE-CHECKLIST.md`에 Phase 0~5로 통합 정리됨 (2026-05-30)**. 아래는 raw 목록.

- 🆕 **Airtable Automation 4건** (가이드: `airtable/automation-setup-guide.md`): #2, #2.5, #5, #7b 트리거 설정. payload `{record_id}`.
- **Xero Developer Console**: INVOICE.UPDATE webhook 등록 (URL `https://youngfoods.app.n8n.cloud/webhook/xero-invoice-update-v1`) + signing key 발급 → n8n 환경변수 `XERO_WEBHOOK_KEY` 설정 (없으면 stub mode로 자동 통과).
- **Tally**: 온보딩 폼 `Me75K8` Integrations → Webhooks 등록 (URL `/webhook/tally-onboarding-intake-v1`) + signing secret 발급.
- **Tally redirectOnCompletionUrl**: `https://youngfoods.app.n8n.cloud/webhook/onboarding-redirect-v1?response_id={response_id}` (#8 빌드 후).
- **HubSpot Workflow `Company create → webhook` (옵션 A)**: Company 생성 시 → n8n #0 webhook POST. tier 안 되면 polling(옵션 B) 그대로.
- **Xero Branding theme**: footer/payment instructions에 회사 계좌 (BSB·계좌번호) 입력. #2.5 Prepay/COD 인보이스 PDF에 자동 노출.
- ~~Airtable 고객 credit_limit 필드~~ ✅ 완료 (2026-05-30, `fldmZiwCKyybPcSCI`). 값 입력은 온보딩 시 영업/admin.
- **#7b 가동 전**: Gmail OAuth2 발신 주소가 응대용(orders@ 등)인지 확인 + HubSpot Private App에 engagement write scope(`crm.objects.contacts.write`) 필요시 추가.
- **GoCardless 계정** + API key → n8n credential `gocardless-api-key` (#8 빌드 unlock).
- **ClickSend 알파태그** `YoungFoods` 승인 확인 (~2026-05-30, #6a/b unlock).
- **HubSpot 테스트 Company `266741551606` archive** (테스트 데이터 정리).

### ⏳ 다음 픽업 시 추천 작업 순서 (집/맥북)

1. ~~#3-polling 빌드~~ ✅ (2026-05-30, `atQNwPt7B3QUBLBi`). ~~컷오프 timezone~~ ✅. ~~#7b Phase 3 환영 이메일~~ ✅ (Gmail+CRM 로깅).
2. **워크플로우 manual test** — 사용자 손작업 일부 완료 후 #5 → #7a → #7b → #2.5 → #2 → #3 순으로 1건씩 trigger. (#7b 테스트 시 Gmail 발신·HubSpot engagement scope 확인)
3. **운영 가동 체크리스트 문서**: 사용자 가동 직전 단계별 안내 (B트랙 사용자 UI 작업 = Airtable Automation 4건 + Xero/Tally webhook·signing key + Xero Branding theme + GoCardless/ClickSend).
4. **#3-poll·#1·#7b 등 manual 실행 검증** (특히 #1 컷오프 DST 계산 실측, #3-poll HMAC 패리티).

### ⏳ 그 이후
- `YoungFoods` 알파태그 승인 확인 (대시보드/이메일, ~2026-05-30)
- n8n `tally-webhook-secret` credential 등록 (6 완료 후)
- Airtable UI 마무리 작업 (lookup convert·single link convert) — [[airtable-base]] 참조
- 본격 n8n 워크플로우 빌드 시작 (#1부터)

## 다음 픽업 시 추천 순서 (집/맥북, 2026-05-29 저녁)

1. **컴퓨터 픽업** — 회사 Windows에서 commit·push 완료 (마지막 commit `9b448ab`). 집 맥북에서 `cd yflifecycle && git pull`. 정션 이미 살아있으면 setup-memory-link.ps1 skip.
2. **#0 활성화 전 확인사항 (가장 먼저)** — n8n UI에서 워크플로우 id `lNABcJuKPABoqQf0` 열어 review:
   - **HubSpot Private App scope 확인 필수** — credential 이름이 "HubSpot Private App (companies read)"라 read만 있을 가능성 큼. **Company PATCH (magic_token 갱신) 실패하면 scope `crm.objects.companies.write` 추가 필요**. HubSpot Settings → Integrations → Private Apps → 해당 App → Scopes → CRM → Companies → Write 체크 → 토큰은 그대로 (값은 안 바뀜).
   - **테스트 방법**: n8n UI에서 "Execute Workflow" 수동 실행 (Schedule 안 켜고도 수동 가능). 최근 10분 내 신규 Company 0건이면 0 처리 (정상). HubSpot에서 테스트 Company 1건 만들고 다시 수동 실행 → 6 노드 다 통과하는지, Airtable에 행 생기는지, HubSpot magic_token property에 값 박히는지 확인.
   - **기존 Company bootstrap**: createdate filter 기반이라 #0 활성화 이전에 생성된 HubSpot Company는 처리 안 됨. 필요시 임시로 Search 노드의 filter value를 `Date.now() - 30 * 24 * 60 * 60 * 1000` (30일 전) 같이 확장 → 수동 fire → 다시 10min lookback으로 복원.
   - 테스트 OK → workflow Activate 토글 ON → 5분 cron 시작
3. **그 다음 작업 후보** (Claude 진행 가능):
   - **#7a 빌드** (Tally webhook → HubSpot 매칭 → Airtable staging) — 큼, Tally 폼 먼저 있어야 의미. 폼 없으면 Tally MCP로 만들기부터.
   - **#7b 빌드** (Airtable Onboarding Submissions.status=approved → Xero Contact + Airtable update + 기존 hold 오더 release + 환영 이메일) — 큼.
   - **#1 Slack alert 노드 추가** (pre-onboarding/게스트 알림) — 중간.
   - **Production Plan/Schedule 실제 운영 시작** (스키마는 이미 있음, 실 데이터 입력) — 영업·생산 협의 필요.
4. **사용자 손 작업** (Claude 진행 불가):
   - Tally signing secret 발급 (#7a 빌드 전제)
   - Airtable 자동화 `Onboarding Submissions.status=approved → n8n #7b webhook` (#7b URL 확보 후)
   - Xero Branding theme 회사 계좌 입력 (#2.5 가동 전)
   - ClickSend `YoungFoods` 알파태그 승인 확인 (~2026-05-30, #6 가동 전)
   - HubSpot Private App scope 확장 (위 2번에서 필요 시)

## 활성화 전 워크플로우 점검 체크리스트

| 워크플로우 | id | 활성화 전 점검 |
| --- | --- | --- |
| #0 (HubSpot→Airtable+토큰) | `lNABcJuKPABoqQf0` | ✅ **활성화 완료** (2026-05-29 저녁). 5min cron 가동 중. |
| #1 (주문 제출) | `twZw1wv3Fc1iJdeO` | Webhook Bearer 토큰 (n8n credential `order-intake-token` id `dY4EiGLUEsseUd4h`)을 주문 사이트에 박기, 컷오프 검증·중복 의심 Slack 등 미구현 인지 |
| #7a (Tally onboarding intake) | `ylRJHUCFQ9RmCa6R` | 24 노드, Forward 가드 + Slack + HubSpot PATCH 모두 추가됨. Webhook URL `https://youngfoods.app.n8n.cloud/webhook/tally-onboarding-intake-v1`를 Tally `Me75K8` Integrations > Webhooks에 박기. Tally signing secret 발급 후 HMAC 검증 노드 추가 (next iter). 수동 테스트 = Tally URL에 `?hubspot_id=<test>&email_prefill=foo@bar.com` prefill → 폼 작성·제출 → Airtable staging 행 + Slack `#ops-onboarding` 알림 + matched 시 HubSpot Company `onboarding=form submitted` 확인. |

## 다음 작업 후보 (사용자가 turn 시작 시 택일):
- n8n 워크플로우 #6c 후보 (이메일 복구 — order-site 정책상 phone OR email 모두 허용) — 운영 가동 후 수요 보고 결정
- Production Plan/Schedule 실제 운영 시작 (스키마 있음, 실 데이터 입력)
- 본격 가동 단계로 전환 (운영 액션 집행 + 실제 시스템 구현)
- 가동 전 사용자 UI 액션 남은 것: Airtable 자동화 (`Onboarding Submissions.status=approved → n8n #7b webhook`, #7b 빌드 후 URL 박기), Tally signing secret, Xero Branding theme 계좌, HubSpot Private App scope (필요시), ClickSend `YoungFoods` 승인 확인

남은 운영 액션(설계 외): n8n에 Custom Connection 키·ops-* 채널 ID 등록 (clientSecret은 placeholder인 상태, 사용자가 n8n UI에서 직접 입력하도록 안내함 — [[xero-account]]), Xero scope 문제 진단 대기 ([[xero-account]]).

진행 방식은 [[yf-conversation-style]] 참조.
