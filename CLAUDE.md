# Young Foods — B2B 주문 시스템 설계 컨텍스트

> **상태**: 설계 초안 v0.2 (대화로 정교화 진행 중)
> **마지막 업데이트**: 2026-05-27
> **다음 작업**: 남은 미결 사항 확정 → 개발 요구사항 명세로 전환
> **관련 문서**: 품목 상세 `products.md` · (예정) 생산·재고 모듈 `production-planning.md`

---

## 프로젝트 개요

냉동식품 B2B 주문 시스템. 식당·급식업체 등을 대상으로 온라인 주문 사이트를 운영.

- 생산 방식: Make-to-stock — 재고는 출하를 막지 않음. 대신 생산 계획을 굴려서 재고를 유지 (상세: `production-planning.md`)
- 품목: 냉동 제품 3종 — 상세는 **Airtable 제품 테이블(SoT)** 및 `products.md` 참조
- 결제: Invoice 기반 (payment term 부여 후 사후 정산). 인보이스는 **dispatch(배송) 시점**에 발행
- 사업 지역: 호주 (.au) — DD는 BECS 스킴, 개인정보는 Australian Privacy Act(APPs) 적용

---

## 핵심 설계 원칙

1. **로그인 없는 주문**: ID/비밀번호 없음. 고유 매직 링크(랜덤 토큰)가 인증 대신
2. **주문은 신원보다 먼저**: 등록 전 게스트도 주문 가능. 신원 확정은 사후 처리
3. **시스템별 단일 책임**: 각 시스템은 가장 잘하는 일만. 데이터 소유권 명확히 분리
4. **결제 가능 = 온보딩 완료**: Payment term 보유 = 즉시 출하 자격
5. **분실 전제 설계**: 매직 링크 분실을 전제로 여러 겹의 재접근 수단 구비

---

## 시스템 역할 분담

| 시스템         | 역할                  | 책임 범위                                                                              |
| -------------- | --------------------- | -------------------------------------------------------------------------------------- |
| **HubSpot**    | CRM 전용              | 고객 관계 관리, 영업 파이프라인, 온보딩, 신규 고객 발굴. 주문 데이터 저장 안 함        |
| **Airtable**   | 주문 운영 + 통합 허브 | 오더/라인아이템/제품/고객 테이블, 주문 상태 관리, **시스템 간 ID 매핑 허브**           |
| **Xero**       | 재무 진실             | 인보이스 발행, 결제 상태 SoT, 미수금·고객 은행정보·statement·payroll·GST·bank rec      |
| **GoCardless** | 자동 수금             | BECS Direct Debit 기반 자동 수금. Xero 네이티브 연동으로 인보이스 due date에 자동 수금 |

### 연결 원칙 — Airtable 고객 테이블이 매핑 허브

각 시스템의 외부 ID를 사슬로 엮지 않고, **Airtable 고객 레코드가 모든 키를 나란히 보관**한다.

```
Airtable 고객 레코드 (내부 단일 진실)
  ├── HubSpot 고객 ID   (영업/CRM)
  ├── Xero ContactID    (재무)
  ├── 매직/복구 토큰     (주문 인증)
  └── 상호·담당자·연락처·기본 배송지·payment term
```

- 모든 오더 행은 Airtable 고객 레코드에 연결됨. 게스트는 미연결(미배정) 상태.

### 데이터 소유권 요약

- "누가 우리 고객인가" → HubSpot
- "무엇을 주문했는가" → Airtable
- "제품/가격/재고 기준값은" → Airtable 제품 테이블
- "돈을 냈는가" → Xero
- "어떻게 수금하는가" → GoCardless / Xero

---

## 두 가지 진입 경로

### 정문 A — 매직 링크 (알려진 고객)

- 영업이 온보딩시킨 고객. 고유 토큰이 담긴 링크로 진입
- 예: `order.example.com/o/a8f3k2x9` (토큰은 추측 불가능한 랜덤값)
- 토큰이 비밀번호 역할. 내부에서 **Airtable 고객 레코드와 1:1 매핑**
- 화면에 가게명·담당자명·기본 배송지·payment term prefill
- **보안**: URL에 내부 ID(HubSpot/Xero/레코드) 직접 노출 금지

### 정문 B — 글로벌 사이트 (게스트/셀프)

- 토큰 없음, 고객 레코드 미연결, 온보딩 미완료
- 최소 정보(상호·연락처)만 입력하는 간이 폼
- Airtable에 `고객 미배정`으로 저장

### 게스트 → 정식 고객 승격 (즉시 온보딩 원칙)

1. 게스트 주문 → Airtable 미배정 + payment term 없음으로 저장 → **Slack 알림**
2. 영업에게 알림 → 영업이 연락
3. 온보딩 폼 발송 (payment term 선택 + DD면 GoCardless mandate 포함)
4. 고객 작성·승인 → HubSpot 신규 고객 생성, 고객 ID 발급, Xero ContactID 발급, payment term 확정
5. **미배정 주문 소급 매칭**: 상호 + 배송주소로 **수동 매칭** (Slack 알림으로 처리) → 고객 레코드 연결
6. 고유 매직 링크(토큰) 발급 → 다음부터 정문 A 진입

**선결제 예외**: DD 부담스럽거나, 신용계정 부여 곤란한 게스트 → 선결제/COD 트랙

---

## 토큰(매직 링크) 수명주기

- **모델: 영구 토큰 + 신고 시 폐기·재발급** (주기적 회전 안 함)
- 폐기·재발급 트리거: 분실/도난 신고, 직원 퇴사, 폰 분실 등
- **재발급 시 옛 토큰 즉시 무효화** (유효 토큰이 누적되지 않게)
- QR 스티커도 같은 영구 토큰을 가리킴 → 토큰이 영구라 QR이 죽지 않음

---

## 결제 프로세스

출하를 좌우하는 변수 = **payment term 유무 + hold 여부**

### Payment term별 인보이스 발행 시점

| 고객 유형         | 인보이스 발행 시점 | 수금 방식                                                                    |
| ----------------- | ------------------ | ---------------------------------------------------------------------------- |
| **GoCardless DD** | **dispatch 시**    | 인보이스 due date를 주간 수금 요일로 맞춰 Xero–GoCardless 네이티브 자동 수금 |
| **7-day credit**  | **dispatch 시**    | 고객이 직접 결제 (7일 신용)                                                  |
| **선결제 / COD**  | **dispatch 전**    | 결제 확인 후 출하 (Hold)                                                     |

- **오더 1개 = Xero 인보이스 1개** (1:1). DD 고객은 이 인보이스들이 주중 쌓였다가 수금 요일에 자동 수금됨.
- DD 수금은 **due date 기준 자동**이라 파일 업로드/수동 입력 불필요. mandate(BECS DDR)는 **온보딩 시 1회 동의**.
- **주의(BECS)**: DD는 제출 후 약 2~3 영업일 뒤 정산. 수금 요일 직전 발행 인보이스는 다음 사이클로 밀릴 수 있음 → 수금 요일은 이 리드타임 감안해 설정.

### 선결제/COD 고객 (dispatch 전 결제)

- 출하 상태 = **Hold** → 결제 확인 후 출하
- 결제 경로 ①: 직접 계좌이체 → 입금 확인 후 Xero 인보이스 Paid 처리
- 결제 경로 ②: Xero 인보이스 "Pay now" 링크 → 온라인 결제 → 자동 Paid (운영 권장)

### Hold 모델 (2단위)

| Hold 종류                   | 발생 원인                                   | 단위      | 해제              |
| --------------------------- | ------------------------------------------- | --------- | ----------------- |
| **고객 단위 hold**          | credit limit 초과, outstanding invoice 문제 | 고객 전체 | Xero 상태 해소 시 |
| **오더/인보이스 단위 hold** | 일부 COD 고객, 선결제                       | 개별 오더 | 결제 확인 시      |

**출하 가능 = NOT(고객 hold) AND NOT(오더 hold)**

- ⚠️ "credit limit 초과"는 Xero가 push 이벤트로 주지 않음 → outstanding 합계 vs credit limit **비교 계산을 별도 로직(n8n/Airtable)에서 직접** 수행해야 함.

### Xero ↔ Airtable 결제 동기화

- `Airtable → Xero`: **dispatch 시** Xero 인보이스 생성 (선결제/COD는 dispatch 전). line item 동반.
- `Xero → Airtable`: 인보이스 Paid → 결제 상태 Paid + (선결제/COD면) Hold 해제. 고객 hold 상태도 동기화.
- **핵심**: Xero webhook으로 실시간 감지 + **주기적 reconciliation 폴백** (webhook 유실 대비)

---

## 주문 페이지 설계

품목 3종만 있어서 매우 단순한 UI. 한 화면에서 주문 완결.

### 매직 링크 진입 시

- 상단 강조 배너: "○○○님, △△식당 전용 주문 페이지입니다"
- 가게명, 담당자명, 이메일, 기본 배송지, payment term 표시
- "내 가게 정보가 아닌가요? 담당 영업사원에게 연락해 주세요" 안내

### 이번 주문만 다른 배송지

Airtable에 3개 필드로 관리:
| 필드 | 내용 |
|---|---|
| 기본 배송지 | 고객 등록 주소 (불변) |
| 이번 주문 배송지 | 이번 건만 다를 때 |
| 실제 배송지 | 수식 필드 — 이번 주문 배송지 있으면 그것, 없으면 기본 배송지 |

### 글로벌 진입 시 (게스트)

- 상단 배너 없음. 빈 입력 폼 (상호·연락처)
- "이미 등록된 가게이신가요? 전화번호/이메일로 내 전용 페이지 찾기" 안내

### 주문 컷오프

- **오더 컷오프 = 배송 전날 12pm**. 컷오프가 닫히면 익일(D+1) dispatch 수량 확정.

---

## 매직 링크 분실 대응 (4겹 방어)

| 대책                                      | 작동 시점                | 손이 가는 정도                |
| ----------------------------------------- | ------------------------ | ----------------------------- |
| 주문 확인 문자/이메일에 링크 동봉         | 매 주문마다 자동         | 거의 없음                     |
| 주문 완료 화면에서 홈 화면 추가 유도      | 주문 직후                | 없음 (PWA, iOS는 안내만 가능) |
| 글로벌 사이트에서 번호/이메일로 링크 받기 | 분실 후 재진입 시        | 없음 (자동 조회·발송)         |
| QR 스티커 (2회+ 재요청 고객)              | 재요청 누적 시 자동 추천 | 영업 방문 시 부착             |

- **핵심 통찰**: 사장님들은 자기 가게 전화번호를 절대 잊지 않는다 → 전화번호로 링크 재발급
- **재발급 보안 규칙**:
  - 전화번호 **또는** 이메일 중 하나로 조회 가능
  - 링크는 **반드시 파일에 등록된 그 연락처로만** 발송 (사용자가 지정한 다른 곳으로 발송 불가)
  - 매칭 실패해도 존재 여부 노출 금지 ("등록돼 있다면 발송했습니다" 식 동일 응답)
  - **rate limit** 필수 (SMS 폭탄·enumeration 차단)
- QR 트리거: 재요청 2회 이상 → Airtable 자동화로 영업에 Slack 알림 + QR 스티커 추천 플래그

---

## Airtable 데이터 설계 (초안)

### 오더 테이블 주요 필드

| 필드             | 타입          | 설명                                                        |
| ---------------- | ------------- | ----------------------------------------------------------- |
| 주문번호         | Auto number   | ORD-1001 형식                                               |
| 고객             | Link          | Airtable 고객 레코드 연결 (비어 있으면 미배정/게스트)       |
| 고객 상태        | Single select | 등록완료 / 미배정                                           |
| Payment term     | Single select | 7-day credit / GoCardless DD / 선결제·COD / (없음)          |
| 상호             | Text          | 게스트는 직접 입력                                          |
| 연락처           | Phone         | 분실 복구·매칭에 사용                                       |
| 라인아이템       | Link          | 라인아이템 테이블 연결 (품목별 수량은 자식 테이블에서 관리) |
| 기본 배송지      | Text          | 등록 주소(불변)                                             |
| 이번 주문 배송지 | Text          | 이번 건만 다를 때                                           |
| 실제 배송지      | Formula       | 이번 주문 배송지 우선, 없으면 기본 배송지                   |
| 출하 상태        | Single select | 접수 / Hold / 출하완료 등                                   |
| 오더 Hold        | Checkbox      | 오더/인보이스 단위 hold (COD·선결제)                        |
| 결제 상태        | Single select | 미결제 / Paid (Xero에서 동기화)                             |
| Xero 인보이스 ID | Text          | Xero 인보이스 연결 (dispatch 시 생성)                       |

### 라인아이템 테이블 (신규)

- 오더(1) ──< 라인아이템(\*) 구조. picking(피킹 리스트)·Xero 인보이스 line 매핑에 사용.
- 필드: 오더(link), 제품(link → 제품 테이블), 수량, 단가(제품 테이블 lookup)

### 제품 테이블 (SoT)

- SKU / 품목명 / 단위 / 박스당 수량 / 단가 / MOQ / Xero Item Code
- **Target stock / Safety stock** (생산·예측 모듈이 lookup으로 사용)
- 사람용 스냅샷: `products.md`

### 고객 테이블

- **HubSpot 고객 ID + Xero ContactID + 매직/복구 토큰** (매핑 허브 키)
- 상호 / 담당자 / 이메일 / 연락처 / 기본 배송지 / payment term
- 고객 Hold (Xero에서 동기화: credit limit 초과·outstanding 문제)
- 링크 재요청 횟수 (QR 스티커 트리거용)
- QR 스티커 추천 플래그 / 발급 여부

### 유용한 뷰

- **미배정 큐**: 고객 미배정 → 영업 신규 리드 전환 대기열
- **Hold 큐**: Hold 걸린 주문 → 결제/신용 대기
- **QR 추천 큐**: QR 스티커 추천 플래그 = true → 영업 전달 대기

---

## 시스템 연동 지점

| 출발         | 도착             | 내용                                                          |
| ------------ | ---------------- | ------------------------------------------------------------- |
| 주문 사이트  | Airtable         | 주문 제출 → 오더 + 라인아이템 행 생성                         |
| Airtable     | Xero             | **dispatch 시** 인보이스 생성 (payment term별 분기)           |
| Xero         | Airtable         | 인보이스 Paid → 결제 Paid + Hold 해제 / 고객 hold 상태 동기화 |
| GoCardless   | Xero             | DD 수금 결과 자동 반영·대사                                   |
| HubSpot/Xero | Airtable         | 신규 고객 → ID 매핑·토큰 발급 (승격 시)                       |
| Airtable     | 영업(Slack)      | 미배정 주문·소급 매칭·중복 주문·주문 취소·QR 추천 알림        |
| 주문 사이트  | 문자 발송 서비스 | 분실 복구 링크 문자, 주문 확인 문자                           |

### 예외 처리 (모두 Slack 알림 → 수동 조치)

- **중복 주문 의심** (같은 고객 + 동일 라인아이템 + N분 이내) → Slack → admin 확인. 멱등키로 1차 예방.
- **게스트 소급 매칭** (상호+주소) → Slack → 영업 수동 매칭.
- **주문 취소** → Slack → Xero 수동 조치.
  - dispatch 전 취소: 인보이스 아직 없음 → Airtable 상태만 "취소". Xero 조치 불필요.
  - dispatch 후 취소/반품: 인보이스 발행됨 → 수동 **credit note**.

**연동 구현 방식**: n8n으로 새로 구성 예정 (n8n MCP 연결됨). 세부 미결.

---

## 미결 사항 (결정 필요)

| 항목                          | 선택지 / 메모                                                            |
| ----------------------------- | ------------------------------------------------------------------------ |
| 전용 페이지 정보 노출 범위    | 이메일·주소 전체 노출 vs 부분 마스킹                                     |
| 주문 검토/완료 화면           | 제출 전 확인 단계 유무, 완료 화면 구성                                   |
| 선결제 시 정보 수정 허용 범위 | 배송지 수정 허용, 가게명 고정 등                                         |
| QR 재요청 카운트 기준         | 링크 재요청 횟수만 vs 링크 클릭 포함 합산                                |
| 문자 발송 서비스              | 호주 기준: Twilio 등 (국내 서비스 불가)                                  |
| 연동 구현 방식 세부           | n8n 워크플로우 구성 (트리거·노드 설계)                                   |
| 온보딩 폼 플랫폼              | HubSpot Forms vs Tally vs 별도. GoCardless는 Billing Request Flow로 연결 |
| 고객별 차등가 / GST 표기      | `products.md` 참조 — 단일가 vs 고객별가, GST 포함/별도                   |
| DD 주간 수금 요일             | BECS 리드타임 감안해 확정                                                |

---

## 확정된 주요 결정 (대화 정교화 결과)

- 토큰: **영구 + 신고 시 폐기·재발급** / QR은 영구 토큰 지시
- 인보이스: **dispatch 시 발행**, payment term별 분기 (선결제/COD만 dispatch 전)
- DD: **Xero–GoCardless 네이티브 자동 수금**, due date = 주간 수금 요일, mandate는 온보딩 1회
- 키 구조: **Airtable 고객 테이블이 매핑 허브** (HubSpot ID + Xero ContactID + 토큰)
- Hold: **2단위** (고객 단위 / 오더 단위), 출하 = NOT(고객) AND NOT(오더)
- 라인아이템: **자식 테이블**로 관리 (품목 3컬럼 방식 폐기)
- 제품/재고 기준값 SoT: **Airtable 제품 테이블**
- forecast: **D+1 확정 주문 / D+2~14 historical 추정** (컷오프 = 배송 전날 12pm)
- 예외(중복·소급매칭·취소): Slack 알림 → 수동 처리

---

## 다음 대화에서 이어갈 내용

1. 남은 미결 사항 확정 (위 표)
2. `production-planning.md` 작성 — 재고·생산 예측 모듈 상세 설계
3. 온보딩 폼 상세 설계 (+ GoCardless Billing Request Flow 연결 방식)
4. n8n 워크플로우 설계 (Xero webhook ↔ Airtable, dispatch 인보이스 생성, DD 등)
5. 개발 요구사항 명세로 전환
