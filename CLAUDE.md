# Young Foods — B2B 주문 시스템 설계 컨텍스트

> **상태**: 설계 초안 v0.1 (대화로 정교화 진행 중)
> **마지막 업데이트**: 2026-05-25
> **다음 작업**: 각 섹션의 미결 사항을 대화로 확정한 뒤 개발 요구사항 명세로 전환 예정

---

## 프로젝트 개요

냉동식품 B2B 주문 시스템. 식당·급식업체 등을 대상으로 온라인 주문 사이트를 운영.
- 생산 방식: Make-to-stock (재고 유지 지향) — 재고는 출하의 제약이 아님
- 품목: 냉동 제품 3종 (품목명·단위·최소 주문 수량 미결)
- 결제: Invoice 기반 (payment term 부여 후 사후 정산)

---

## 핵심 설계 원칙

1. **로그인 없는 주문**: ID/비밀번호 없음. 고유 매직 링크(랜덤 토큰)가 인증 대신
2. **주문은 신원보다 먼저**: 등록 전 게스트도 주문 가능. 신원 확정은 사후 처리
3. **시스템별 단일 책임**: 각 시스템은 가장 잘하는 일만. 데이터 소유권 명확히 분리
4. **결제 가능 = 온보딩 완료**: Payment term 보유 = 즉시 출하 자격
5. **분실 전제 설계**: 매직 링크 분실을 전제로 여러 겹의 재접근 수단 구비

---

## 시스템 역할 분담

| 시스템 | 역할 | 책임 범위 |
|--------|------|-----------|
| **HubSpot** | CRM 전용 | 고객 관계 관리, 영업 파이프라인, 온보딩, 신규 고객 발굴. 주문 데이터 저장 안 함 |
| **Airtable** | 주문 운영 | 오더 테이블(주문 저장), 주문 상태 관리, 고객 운영 데이터 |
| **Xero** | 재무 진실 | 인보이스 발행, 결제 상태의 단일 진실 공급원(source of truth) |
| **GoCardless** | 자동 수금 | Direct Debit 기반 주간 인보이스 자동 수금 |

**연결 원칙**: HubSpot의 고유 고객 ID가 네 시스템을 잇는 단일 열쇠.
Airtable 오더 테이블의 모든 주문 행에 이 고객 ID가 있어야 함.

### 데이터 소유권 요약
- "누가 우리 고객인가" → HubSpot
- "무엇을 주문했는가" → Airtable
- "돈을 냈는가" → Xero
- "어떻게 수금하는가" → GoCardless / Xero

---

## 두 가지 진입 경로

### 정문 A — 매직 링크 (알려진 고객)
- 영업이 온보딩시킨 고객. 고유 토큰이 담긴 링크로 진입
- 예: `order.example.com/o/a8f3k2x9` (토큰은 추측 불가능한 랜덤값)
- 토큰이 비밀번호 역할. 내부에서 HubSpot 고객 ID와 1:1 매핑
- 화면에 가게명·담당자명·기본 배송지·payment term prefill
- **보안**: URL에 HubSpot ID 직접 노출 금지 (예: `/o/12345` 금지)

### 정문 B — 글로벌 사이트 (게스트/셀프)
- 토큰 없음, HubSpot ID 없음, 온보딩 미완료
- 최소 정보(상호·연락처)만 입력하는 간이 폼
- Airtable에 `HubSpot ID = 공란(미배정)`으로 저장

### 게스트 → 정식 고객 승격 (즉시 온보딩 원칙)
1. 게스트 주문 → Airtable 미배정 + payment term 없음으로 저장
2. 영업에게 알림 → 영업이 연락
3. 온보딩 폼 발송 (payment term 선택 포함)
4. 고객 작성·승인 → HubSpot 신규 고객 생성, 고객 ID 발급, payment term 확정
5. Airtable 미배정 주문에 고객 ID 소급 입력 → 정식 흐름 합류
6. 고유 매직 링크(토큰) 발급 → 다음부터 정문 A 진입

**선결제 예외**: DD 부담스럽거나, 신용계정 부여 곤란한 게스트 → 선결제 트랙

---

## 결제 프로세스

출하를 좌우하는 유일한 변수 = **payment term 유무**

### 온보딩 완료 고객 (payment term 있음)
| Payment term | 설명 |
|---|---|
| 7-day credit account | 주문 후 7일 신용 거래. 출하는 결제 기다리지 않음 |
| GoCardless DD | 주 1회 합산 인보이스 Direct Debit 자동 수금. 출하는 결제 기다리지 않음 |

### 선결제 예외 고객 (payment term 없음)
- 주문 상태 = **Hold** → 결제 확인 후 출하
- 결제 경로 ①: 직접 계좌이체 → 입금 확인 후 Xero 인보이스 Paid 처리
- 결제 경로 ②: Xero 인보이스 "Pay now" 링크 → 온라인 결제 → 자동 Paid (운영 권장)

### Xero ↔ Airtable 결제 동기화
- `Airtable → Xero`: 주문 접수 시 Xero 인보이스 생성
- `Xero → Airtable`: 인보이스 Paid 시 Airtable 결제 상태 Paid + (선결제면) Hold 해제
- **핵심**: Xero webhook으로 실시간 감지

---

## 주문 페이지 설계

품목 3종만 있어서 매우 단순한 UI. 한 화면에서 주문 완결.

### 매직 링크 진입 시
- 상단 강조 배너: "○○○님, △△식당 전용 주문 페이지입니다"
- 가게명, 담당자명, 이메일, 기본 배송지, payment term 표시
- "내 가게 정보가 아닌가요? 담당 영업사원에게 연락해 주세요" 안내

### 이번 주문만 다른 배송지
Airtable에 3개 필드로 관리:
| 필드 | 내용 |
|---|---|
| 기본 배송지 | 고객 등록 주소 (불변) |
| 이번 주문 배송지 | 이번 건만 다를 때 |
| 실제 배송지 | 수식 필드 — 이번 주문 배송지 있으면 그것, 없으면 기본 배송지 |

### 글로벌 진입 시 (게스트)
- 상단 배너 없음. 빈 입력 폼 (상호·연락처)
- "이미 등록된 가게이신가요? 전화번호/이메일로 내 전용 페이지 찾기" 안내

---

## 매직 링크 분실 대응 (4겹 방어)

| 대책 | 작동 시점 | 손이 가는 정도 |
|---|---|---|
| 주문 확인 문자/이메일에 링크 동봉 | 매 주문마다 자동 | 거의 없음 |
| 주문 완료 화면에서 홈 화면 추가 유도 | 주문 직후 | 없음 (PWA) |
| 글로벌 사이트에서 번호/이메일로 링크 받기 | 분실 후 재진입 시 | 없음 (자동 조회·발송) |
| QR 스티커 (2회+ 재요청 고객) | 재요청 누적 시 자동 추천 | 영업 방문 시 부착 |

- **핵심 통찰**: 사장님들은 자기 가게 전화번호를 절대 잊지 않는다 → 전화번호로 링크 재발급
- QR 트리거: 재요청 2회 이상 → Airtable 자동화로 영업에 Slack 알림 + QR 스티커 추천 플래그

---

## Airtable 데이터 설계 (초안)

### 오더 테이블 주요 필드
| 필드 | 타입 | 설명 |
|---|---|---|
| 주문번호 | Auto number | ORD-1001 형식 |
| HubSpot 고객 ID | Text | 비어 있으면 미배정(게스트) |
| 고객 상태 | Single select | 등록완료 / 미배정 |
| Payment term | Single select | 7-day credit / GoCardless DD / (없음) |
| 상호 | Text | 게스트는 직접 입력 |
| 연락처 | Phone | 분실 복구·매칭에 사용 |
| 품목별 수량 | Number ×3 | 3개 품목 각각 |
| 기본 배송지 | Text | 등록 주소(불변) |
| 이번 주문 배송지 | Text | 이번 건만 다를 때 |
| 실제 배송지 | Formula | 이번 주문 배송지 우선, 없으면 기본 배송지 |
| 출하 상태 | Single select | 접수 / Hold / 출하완료 등 |
| 결제 상태 | Single select | 미결제 / Paid (Xero에서 동기화) |
| Xero 인보이스 ID | Text | Xero 인보이스 연결 |

### 고객 테이블
- HubSpot 고객 ID (연결 키)
- 매직 링크 토큰 (추측 불가능한 랜덤값)
- 상호 / 담당자 / 이메일 / 연락처 / 기본 배송지 / payment term
- 링크 재요청 횟수 (QR 스티커 트리거용)
- QR 스티커 추천 플래그 / 발급 여부

### 유용한 뷰
- **미배정 큐**: 고객 상태 = 미배정 → 영업 신규 리드 전환 대기열
- **Hold 큐**: 출하 상태 = Hold → 선결제 대기 주문
- **QR 추천 큐**: QR 스티커 추천 플래그 = true → 영업 전달 대기

---

## 시스템 연동 지점

| 출발 | 도착 | 내용 |
|---|---|---|
| 주문 사이트 | Airtable | 주문 제출 → 오더 행 생성 |
| Airtable | Xero | 주문 → 인보이스 생성 |
| Xero | Airtable | 인보이스 Paid → 결제 상태 Paid + Hold 해제 |
| GoCardless | Xero | DD 수금 결과 반영 |
| HubSpot | Airtable | 신규 고객 → 토큰 발급/매핑 (승격 시) |
| Airtable | 영업(Slack/메일) | 미배정 주문 알림, QR 스티커 추천 알림 |
| 주문 사이트 | 문자 발송 서비스 | 분실 복구 링크 문자, 주문 확인 문자 |

**연동 구현 방식**: Make/Zapier vs n8n vs webhook+API 직접 구현 — **미결**

---

## 미결 사항 (결정 필요)

| 항목 | 선택지 / 메모 |
|---|---|
| 전용 페이지 정보 노출 범위 | 이메일·주소 전체 노출 vs 부분 마스킹 |
| 실제 품목 정보 | 3개 품목의 품목명, 단위(박스/kg), 최소 주문 수량 |
| 주문 검토/완료 화면 | 제출 전 확인 단계 유무, 완료 화면 구성 |
| 선결제 시 정보 수정 허용 범위 | 배송지 수정 허용, 가게명 고정 등 |
| QR 재요청 카운트 기준 | 링크 재요청 횟수만 vs 링크 클릭 포함 합산 |
| 문자 발송 서비스 | 국내: 알리고·솔라피 / 해외: Twilio 등 |
| 연동 구현 방식 | Make/Zapier vs n8n vs webhook+API 직접 구현 |
| 온보딩 폼 플랫폼 | HubSpot Forms vs 별도 폼 |
| GoCardless DD 동의 구간 | 온보딩 폼 안에서 처리 vs 별도 단계 |

---

## 현재 구축된 것

- **n8n 워크플로우 "Platter Order Intake"** (활성화 중)
  - Gmail Trigger → OpenAI 주문 추출 → Parse JSON → Airtable 저장 → Slack 알림
  - 플래터 주문이 아닌 이메일은 자동 필터링됨
  - SOP 문서: `SOP_platter-order-intake.md`

---

## 다음 대화에서 이어갈 내용

1. 미결 사항들을 하나씩 대화로 확정
2. 시스템 연동 구현 방식 결정 (특히 Xero webhook ↔ Airtable)
3. 온보딩 폼 상세 설계
4. 개발 요구사항 명세로 전환